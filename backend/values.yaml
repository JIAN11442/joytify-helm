# =============================================================================
# deployment.yaml
# =============================================================================
app:
  name: api
  replicas: 2
image:
  repository: ghcr.io/jian11442/joytify-api
  tag: a237780f75fff2a6b2419b9acc4076d9c9844f14
  digest: "sha256:726cd1d5f7fbc13a92448a43b0c8e8a59ce223d7ea59162cff81b1408d9dbc5a"
imagePullSecrets:
  name: ghcr-secret # *
container:
  name: api-container
  port: 4004 # *
strategy:
  maxUnavailable: 1
  maxSurge: 1
# =============================================================================
# service.yaml
# =============================================================================
service:
  name: api-service # *
  type: ClusterIP
  port: 4004 # *
  targetPort: 4004 # *
# =============================================================================
# secret.yaml
# =============================================================================

# AWS Credential
awsCredentials:
  name: aws-credentials # *
  accessIdKey: access-key-id # *
  accessSecretKey: secret-access-key # *
# Secret Store
secretStore:
  name: aws-secrets-store
  aws:
    region: ap-northeast-1 # *
# External Secret
externalSecret:
  name: api-secret
  target:
    name: api-secret
    creationPolicy: Owner
  dataFrom:
    extract:
      key: JOYTIFY_APPLICATION_ENVS # *
# =============================================================================
# ingress.yaml
# =============================================================================
ingress:
  enabled: true
  domain: "api.139.59.223.77.nip.io" # *
  clusterIssuer: "letsencrypt-staging" # *
# =============================================================================
# envs
# =============================================================================
envs:
  ORIGIN_APP: "https://139.59.223.77.nip.io"
  API_PORT: 4004
  OFFICIAL_EMAIL:
  TEST_EMAIL: onboarding@resend.dev
