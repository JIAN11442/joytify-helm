# =============================================================================
# deployment.yaml
# =============================================================================
app:
  name: api
  replicas: 2
image:
  repository: ghcr.io/jian11442/joytify-api
  tag: c187670e3d5d5e4e829d305e646d52059dabbebe
  digest: "sha256:109ebf293bcf6151820110ff7edd8dafe05aae57f0254f7e1aefa14603e8a565"
imagePullSecrets:
  name: ghcr-secret # *
container:
  name: api-container
  port: 4004 # *
strategy:
  maxUnavailable: 1
  maxSurge: 1
# =============================================================================
# service.yaml
# =============================================================================
service:
  name: api-service # *
  type: ClusterIP
  port: 4004 # *
  targetPort: 4004 # *
# =============================================================================
# secret.yaml
# =============================================================================

# AWS Credential
awsCredentials:
  name: aws-credentials # *
  accessIdKey: access-key-id # *
  accessSecretKey: secret-access-key # *
# Secret Store
secretStore:
  name: aws-secrets-store
  aws:
    region: ap-northeast-1 # *
# External Secret
externalSecret:
  name: api-secret
  target:
    name: api-secret
    creationPolicy: Owner
  dataFrom:
    extract:
      key: JOYTIFY_APPLICATION_ENVS # *
# =============================================================================
# ingress.yaml
# =============================================================================
ingress:
  enabled: true
  domain: "api.146.190.193.154.nip.io" # *
  clusterIssuer: "letsencrypt-staging" # *
# =============================================================================
# envs
# =============================================================================
envs:
  ORIGIN_APP: "https://146.190.193.154.nip.io"
  API_PORT: 4004
  OFFICIAL_EMAIL:
  TEST_EMAIL: onboarding@resend.dev
